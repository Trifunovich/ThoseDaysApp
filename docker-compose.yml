version: '3.8'

services:
  postgres:
    image: postgres:15-alpine
    container_name: those-days-postgres
    environment:
      POSTGRES_USER: ${DB_USER:-postgres}
      POSTGRES_PASSWORD: ${DB_PASSWORD:-postgres}
      POSTGRES_DB: ${DB_NAME:-thosedays}
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ${DB_USER:-postgres}"]
      interval: 10s
      timeout: 5s
      retries: 5
    networks:
      - thosedays-network

  pgadmin:
    image: dpage/pgadmin4:latest
    container_name: those-days-pgadmin
    environment:
      PGADMIN_DEFAULT_EMAIL: admin@thosedays.local
      PGADMIN_DEFAULT_PASSWORD: ${PGADMIN_PASSWORD:-admin}
    ports:
      - "5050:80"
    depends_on:
      - postgres
    networks:
      - thosedays-network

volumes:
  postgres_data:

networks:
  thosedays-network:
    driver: bridge
